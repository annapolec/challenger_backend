%p= @group.name

%p
  = link_to "All groups", groups_path
  = link_to "Edit group", edit_group_path(@group)
  = link_to "Destroy group", group_path, method: :delete

%p
  = link_to "Add users", add_users_group_path(@group)