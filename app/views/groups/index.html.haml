.row.p-t-3
  %h1.text-xs-center My groups
  .col-md-6.col-md-offset-3
    .list-group
      - @groups.each do |group|
        = link_to group.name, group_path(group), class: 'list-group-item'
.row  
  = button_to "New group", new_group_path, class: 'btn btn-info-outline btn-lg center-block m-t-3'